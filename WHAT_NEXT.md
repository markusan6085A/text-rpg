# 🚀 Що робити далі?

## ✅ Інтеграція Backend API завершена!

Тепер потрібно **протестувати**, чи все працює.

<hr style="border: none; border-top: 2px dotted #C9B36B; margin: 20px 0;">

## 📋 Крок 1: Перевірити, що Backend працює

### Запустити Backend сервер:

```bash
cd server
npm run dev
```

Сервер має запуститися на `http://localhost:3000`

### Перевірити, що сервер працює:

Відкрити в браузері:
```
http://localhost:3000
```

Повинно показати JSON з інформацією про сервер та endpoints.

<hr style="border: none; border-top: 2px dotted #C9B36B; margin: 20px 0;">

## 📋 Крок 2: Запустити Frontend (гра)

### В новому терміналі (не закриваючи backend):

```bash
npm run dev
```

Гра має запуститися на `http://localhost:5174` (або інший порт, якщо 5174 зайнятий)

<hr style="border: none; border-top: 2px dotted #C9B36B; margin: 20px 0;">

## 📋 Крок 3: Протестувати інтеграцію

### Тест 1: Реєстрація нового користувача

1. Відкрити гра в браузері
2. Натиснути "Начать игру" або перейти на `/register`
3. Заповнити форму:
   - Логин (5-15 символів)
   - Пароль
   - Раса, Класс, Пол
4. Натиснути "Зарегистрироваться"
5. Перевірити:
   - ✅ Реєстрація пройшла успішно
   - ✅ Персонаж створився в базі даних
   - ✅ Гра перенаправила на `/city`
   - ✅ Герой відображається правильно

### Тест 2: Логін існуючого користувача

1. Відкрити гра в браузері
2. Ввести логін та пароль
3. Натиснути "Войти в игру"
4. Перевірити:
   - ✅ Логін пройшов успішно
   - ✅ Персонаж завантажився з API
   - ✅ Гра перенаправила на `/city`
   - ✅ Герой відображається правильно

### Тест 3: Збереження даних

1. Увійти в гру
2. Зробити якісь зміни (наприклад, одягнути предмет, змінити стати)
3. Перезавантажити сторінку (F5)
4. Перевірити:
   - ✅ Дані збереглися в базі даних
   - ✅ Дані завантажилися після перезавантаження
   - ✅ Герой відображається правильно

<hr style="border: none; border-top: 2px dotted #C9B36B; margin: 20px 0;">

## 🔍 Як перевірити, що дані в базі даних?

### Варіант 1: Prisma Studio (найпростіше)

```bash
cd server
npm run prisma:studio
```

Відкриється браузер з Prisma Studio, де можна побачити всі таблиці та дані.

### Варіант 2: Supabase Dashboard

1. Відкрити https://supabase.com/dashboard
2. Вибрати ваш проєкт
3. Перейти в "Table Editor"
4. Подивитися таблиці `Account` та `Character`

<hr style="border: none; border-top: 2px dotted #C9B36B; margin: 20px 0;">

## ⚠️ Якщо щось не працює:

### Помилка "Failed to fetch" або CORS:

- Переконайтеся, що backend сервер запущений на `http://localhost:3000`
- Переконайтеся, що CORS налаштований в backend

### Помилка "unauthorized":

- Переконайтеся, що ви увійшли в систему
- Перевірте, що token зберігається в localStorage (`auth_token`)

### Помилка з базою даних:

- Переконайтеся, що `.env` файл в `server/` налаштований правильно
- Переконайтеся, що DATABASE_URL вказаний правильно
- Спробуйте запустити міграції:
  ```bash
  cd server
  npm run prisma:migrate
  ```

### Дані не зберігаються:

- Перевірте консоль браузера (F12) на наявність помилок
- Перевірте, що backend сервер працює
- Перевірте, що ви авторизовані (є token в localStorage)

<hr style="border: none; border-top: 2px dotted #C9B36B; margin: 20px 0;">

## 📝 Наступні кроки (після тестування):

1. **Якщо все працює:**
   - ✅ Можна деплоїти на продакшн (див. `DEPLOY.md`)
   - ✅ Або продовжувати розробку

2. **Якщо є проблеми:**
   - 🔍 Перевірити помилки в консолі
   - 🔍 Перевірити логі backend сервера
   - 🔍 Перевірити базу даних

<hr style="border: none; border-top: 2px dotted #C9B36B; margin: 20px 0;">

## 💡 Рекомендації:

1. **Почніть з тестування реєстрації** - це найважливіше
2. **Перевірте, що дані зберігаються** - це критично для роботи гри
3. **Перевірте, що дані завантажуються** - після перезавантаження сторінки

<hr style="border: none; border-top: 2px dotted #C9B36B; margin: 20px 0;">

**Статус:** ✅ Інтеграція завершена, готово до тестування! 🎉
