# 🔧 Перевірка міграцій Prisma

## ❌ Проблема

Реєстрація повертає "Internal Server Error" (500). Бекенд досягає бекенду, але щось не працює.

## 🔍 Можливі причини

1. **Таблиці не створені в базі даних** - потрібні міграції Prisma
2. **Помилка підключення до бази** (але DATABASE_URL вже виправлено)
3. **JWT_SECRET відсутній** на Railway
4. **Помилка в коді реєстрації**

## ✅ Рішення

### Крок 1: Перевірити логи Railway

1. Railway → **Logs**
2. Прокрути до останніх записів (після спроби реєстрації)
3. Шукай помилки:
   - "relation \"Account\" does not exist" - таблиці не створені
   - "JWT_SECRET is missing" - JWT_SECRET відсутній
   - "Can't reach database server" - проблема підключення
   - Інші червоні помилки

### Крок 2: Перевірити JWT_SECRET на Railway

1. Railway → **Settings** → **Variables**
2. Перевір, чи є `JWT_SECRET`
3. Якщо немає - додай:
   - Key: `JWT_SECRET`
   - Value: будь-який довгий ключ (наприклад: `my-super-secret-jwt-key-12345`)

### Крок 3: Запустити міграції (якщо таблиці не створені)

Якщо в логах є "relation \"Account\" does not exist":

**Варіант А: Додати міграції в Build Command**

1. Railway → **Settings** → **Deploy**
2. Знайди **Build Command**
3. Онови на:
   ```
   npm install && npm run prisma:generate && npx prisma migrate deploy && npm run build
   ```
4. Збережи - Railway автоматично перезапустить

**Варіант Б: Через Railway CLI (якщо встановлено)**

```bash
railway run npx prisma migrate deploy
```

## 🎯 Швидке рішення

1. **Перевір логи Railway** - знайди конкретну помилку
2. **Перевір JWT_SECRET** - чи є на Railway
3. **Якщо таблиці не створені** - додай міграції в Build Command

## ❓ Якщо не знаєш, що робити

Надішли логи Railway (останні записи після спроби реєстрації) - там буде конкретна помилка!
