# 🔧 Виправлення: Vercel не деплоїть оновлення з гілки 2025-12-23-zsq5

## ❌ ПРОБЛЕМА:
Vercel деплоїть з гілки `main`, а всі зміни в гілці `2025-12-23-zsq5`. Тому оновлення не показуються на сайті.

## ✅ РІШЕННЯ:

### Варіант 1: Змінити Production Branch в Vercel (РЕКОМЕНДОВАНО)

1. **Відкрити Vercel Dashboard:**
   - https://vercel.com/dashboard
   - Знайти проект `text-rpg`

2. **Перейти в Settings:**
   - Натиснути на проект
   - Перейти на вкладку **"Settings"**
   - Знайти секцію **"Git"**

3. **Змінити Production Branch:**
   - Знайти поле **"Production Branch"**
   - За замовчуванням там `main`
   - Змінити на: `2025-12-23-zsq5`
   - Натиснути **"Save"**

4. **Vercel автоматично задеплоїть з нової гілки:**
   - Зачекати 1-2 хвилини
   - Перевірити вкладку **"Deployments"**
   - Має з'явитися новий deployment з гілки `2025-12-23-zsq5`

---

### Варіант 2: Зробити Redeploy вручну

1. **Відкрити Vercel Dashboard:**
   - https://vercel.com/dashboard
   - Проект `text-rpg` → **"Deployments"**

2. **Знайти останній deployment:**
   - Натиснути на три крапки (⋯) біля deployment
   - Вибрати **"Redeploy"**
   - Виберіть гілку `2025-12-23-zsq5` (якщо є опція)

3. **Або створити новий deployment:**
   - Натиснути **"Create Deployment"**
   - Вибрати гілку: `2025-12-23-zsq5`
   - Натиснути **"Deploy"**

---

### Варіант 3: Змержити зміни в main (якщо хочете залишити main як production)

1. **Змержити гілку в main:**
   ```bash
   git checkout main
   git merge 2025-12-23-zsq5
   git push origin main
   ```

2. **Vercel автоматично задеплоїть з main**

---

## 🔍 ПЕРЕВІРКА ПІСЛЯ ВИПРАВЛЕННЯ:

1. **Перевірити Deployment:**
   - Vercel Dashboard → Deployments
   - Останній deployment має мати commit з гілки `2025-12-23-zsq5`
   - Наприклад: `30fe2980 Fix DELETE endpoint...`

2. **Перевірити на сайті:**
   - Відкрити ваш домен (l2dop.com або vercel.app URL)
   - Hard refresh: `Ctrl + Shift + R`
   - Перевірити, чи з'явилися оновлення

3. **Перевірити в DevTools:**
   - F12 → Network
   - Має бути `limit=10` в запитах чату
   - Повідомлення мають бути зверху

---

## ⚠️ ВАЖЛИВО:

**Якщо ви використовуєте кастомний домен (l2dop.com):**
- Переконайтеся, що Production Branch встановлено на `2025-12-23-zsq5`
- Або змержте зміни в `main` і залиште Production Branch = `main`

**Якщо ви використовуєте Vercel URL (text-rpg-xxx.vercel.app):**
- Можна встановити Production Branch = `2025-12-23-zsq5`
- Або використати Preview URL для тестування

---

## 🎯 РЕКОМЕНДАЦІЯ:

**Найпростіше:** Варіант 1 - змінити Production Branch на `2025-12-23-zsq5`
- Займе 30 секунд
- Vercel автоматично задеплоїть
- Всі оновлення будуть на production
