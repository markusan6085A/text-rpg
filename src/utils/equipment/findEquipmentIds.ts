/**
 * Утиліта для пошуку ID файлів екіпірування
 * 
 * Цей файл допомагає знайти правильні ID файлів для предметів,
 * переглядаючи структуру папок equipment
 */

/**
 * Отримує список всіх доступних ID файлів для раси та статі
 */
export function getAvailableEquipmentIds(
  race: string,
  gender: string,
  view: "front" | "back" | "top" = "front"
): number[] {
  // Це буде використовуватися для майбутньої автоматизації
  // Зараз повертаємо порожній масив
  // В майбутньому можна додати логіку читання файлів з папки
  return [];
}

/**
 * Створює маппінг на основі імен файлів
 * 
 * @example
 * // Якщо файл називається "armor_ng_helmet_0.png"
 * // то можна автоматично створити маппінг
 */
export function createMappingFromFileNames(): Record<string, number> {
  // Майбутня функціональність для автоматичного створення маппінгу
  return {};
}

