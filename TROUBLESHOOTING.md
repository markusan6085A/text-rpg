# 🔍 Troubleshooting: Internal Server Error

## Помилка: "Internal Server Error"

Це означає, що backend сервер повернув помилку 500.

---

## ✅ Крок 1: Перевірити, що Backend запущений

### Відкрити термінал, де запущений backend:

```bash
cd server
npm run dev
```

Backend має запуститися на `http://localhost:3000`

### Перевірити, що сервер працює:

Відкрити в браузері:
```
http://localhost:3000
```

Повинно показати JSON з інформацією про сервер.

---

## ✅ Крок 2: Перевірити логи Backend

Подивіться в термінал, де запущений backend - там мають бути логи помилки.

Типові помилки:
- `DATABASE_URL is missing` - не налаштований .env
- `JWT_SECRET is missing` - не налаштований JWT_SECRET
- `Can't reach database server` - проблема з підключенням до бази даних
- `Prisma Client is not generated` - потрібно запустити `npm run prisma:generate`

---

## ✅ Крок 3: Перевірити .env файл

Переконайтеся, що `server/.env` існує і містить:

```env
DATABASE_URL="postgresql://postgres:ВАШ_ПАРОЛЬ@db.xxxxx.supabase.co:5432/postgres"
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
```

---

## ✅ Крок 4: Перевірити консоль браузера (F12)

1. Відкрити DevTools (F12)
2. Перейти на вкладку "Console"
3. Подивитися, які помилки там

Там може бути більш детальна інформація про помилку.

---

## ✅ Крок 5: Перевірити Network (F12)

1. Відкрити DevTools (F12)
2. Перейти на вкладку "Network"
3. Спробувати зареєструватися знову
4. Знайти запит до `/auth/register`
5. Подивитися на Response - там може бути детальна інформація про помилку

---

## 🔧 Найчастіші проблеми:

### 1. Backend не запущений
**Рішення:** Запустити `cd server && npm run dev`

### 2. .env файл не налаштований
**Рішення:** Створити `server/.env` з DATABASE_URL та JWT_SECRET

### 3. База даних не підключена
**Рішення:** Перевірити DATABASE_URL в .env

### 4. Prisma Client не згенерований
**Рішення:** Запустити `cd server && npm run prisma:generate`

### 5. Міграції не застосовані
**Рішення:** Запустити `cd server && npm run prisma:migrate`

---

**Найпростіший спосіб діагностики:** Подивитися логи в терміналі, де запущений backend сервер!
