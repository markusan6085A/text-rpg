# 🚀 Деплой гри на свій домен

## 📋 Поточний статус:

✅ **Що готово:**
- Frontend (гра) працює локально
- Backend API налаштований (Fastify + Prisma)
- База даних підключена (Supabase PostgreSQL)
- API endpoints для auth та characters

⚠️ **Що ще не зроблено:**
- `persistence.ts` ще використовує `localStorage` (не підключено до backend API)

<hr style="border: none; border-top: 2px dotted #C9B36B; margin: 20px 0;">

## 🎯 Два варіанти:

### Варіант 1: Спочатку закінчити інтеграцію, потім деплой (рекомендовано)

**Переваги:**
- Всі дані будуть зберігатися в базі даних (можна грати з різних пристроїв)
- Повна інтеграція frontend ↔ backend

**Що потрібно:**
1. Оновити `persistence.ts` для використання backend API
2. Додати систему авторизації в UI
3. Протестувати збереження/завантаження даних
4. Потім деплой

**Час:** ~2-3 години розробки

<hr style="border: none; border-top: 2px dotted #C9B36B; margin: 20px 0;">

### Варіант 2: Деплой зараз (localStorage), інтеграцію пізніше

**Переваги:**
- Швидко (можна задеплоїти за 15-30 хвилин)
- Можна побачити гру онлайн

**Мінуси:**
- Дані зберігаються в браузері (не синхронізуються)
- Потрібно буде інтегрувати пізніше

**Що потрібно:**
1. Деплой frontend (Vercel/Netlify)
2. Деплой backend (Railway/Render)
3. Налаштувати змінні оточення
4. Підключити домен

<hr style="border: none; border-top: 2px dotted #C9B36B; margin: 20px 0;">

## 🚀 Як деплоїти (Варіант 2 - швидкий):

### 1. Frontend на Vercel (безкоштовно, найпростіше)

1. **Створити аккаунт на Vercel:**
   - https://vercel.com
   - Реєстрація через GitHub (рекомендовано)

2. **Підключити проєкт:**
   - Натиснути "New Project"
   - Вибрати GitHub репозиторій `text-rpg`
   - Root Directory: `/` (корінь проєкту)
   - Build Command: `npm run build`
   - Output Directory: `dist`
   - Framework Preset: Vite

3. **Environment Variables (якщо потрібно):**
   - `VITE_API_URL` = `https://your-backend-url.railway.app` (після деплою backend)

4. **Deploy!**
   - Vercel автоматично задеплоїть
   - Отримаєте URL типу: `text-rpg.vercel.app`

<hr style="border: none; border-top: 2px dotted #C9B36B; margin: 20px 0;">

### 2. Backend на Railway (безкоштовно $5 credit/mo)

1. **Створити аккаунт на Railway:**
   - https://railway.app
   - Реєстрація через GitHub

2. **Створити новий проєкт:**
   - "New Project" → "Deploy from GitHub repo"
   - Вибрати репозиторій
   - Path: `/server` (папка server)

3. **Налаштувати змінні оточення:**
   - В Railway проєкті → Variables
   - Додати:
     - `DATABASE_URL` = (Supabase connection string з `.env`)
     - `JWT_SECRET` = (ваш секретний ключ)

4. **Налаштувати команди:**
   - Build Command: `npm install && npm run prisma:generate && npm run build`
   - Start Command: `npm start`

5. **Deploy!**
   - Railway автоматично задеплоїть
   - Отримаєте URL типу: `your-app.railway.app`

<hr style="border: none; border-top: 2px dotted #C9B36B; margin: 20px 0;">

### 3. Альтернатива: Render (безкоштовно, трохи повільніше)

**Backend на Render:**
- https://render.com
- "New" → "Web Service"
- Підключити GitHub репозиторій
- Root Directory: `server`
- Build Command: `npm install && npm run prisma:generate && npm run build`
- Start Command: `npm start`
- Environment Variables: `DATABASE_URL`, `JWT_SECRET`

<hr style="border: none; border-top: 2px dotted #C9B36B; margin: 20px 0;">

### 4. Підключення домену (опціонально)

**Для Vercel:**
1. Проєкт → Settings → Domains
2. Додати ваш домен (наприклад, `game.yourdomain.com`)
3. Налаштувати DNS записи (CNAME або A record)

**Для Railway:**
1. Service → Settings → Networking
2. Generate Domain (або підключити custom domain)

<hr style="border: none; border-top: 2px dotted #C9B36B; margin: 20px 0;">

## 📝 Після деплою:

1. **Оновити `vite.config.ts` (якщо потрібно):**
   ```typescript
   export default defineConfig({
     // ...
     server: {
       proxy: {
         '/api': 'http://localhost:3000' // тільки для розробки
       }
     }
   })
   ```

2. **Оновити frontend для використання production API URL:**
   - Створити `src/config/api.ts`:
   ```typescript
   export const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:3000';
   ```

3. **Тестувати:**
   - Відкрити задеплоєну гру
   - Перевірити, що все працює

<hr style="border: none; border-top: 2px dotted #C9B36B; margin: 20px 0;">

## 💡 Рекомендації:

1. **Для початку:** Деплой frontend на Vercel (найпростіше)
2. **Backend:** Railway або Render (обидва безкоштовні для початку)
3. **База даних:** Supabase вже налаштований (залишається як є)

<hr style="border: none; border-top: 2px dotted #C9B36B; margin: 20px 0;">

## ⚠️ Важливо:

- **Якщо зараз деплоїти з localStorage:**
  - Дані не будуть синхронізуватися
  - Кожен гравець матиме локальні дані
  - Пізніше потрібно буде інтегрувати backend API

- **Якщо спочатку інтегрувати backend:**
  - Дані будуть в базі даних
  - Можна грати з різних пристроїв
  - Повна інтеграція готово до деплою

<hr style="border: none; border-top: 2px dotted #C9B36B; margin: 20px 0;">

**Що ви хочете зробити спочатку?** 🤔
