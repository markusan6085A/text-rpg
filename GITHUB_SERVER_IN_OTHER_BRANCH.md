# 🔀 Папка server в іншій гілці

## Проблема:
Папка `server/` запушилася в гілку `2025-12-23-zsq5`, а ви дивитесь на гілку `main` на GitHub.

## Рішення 1: Переключитися на гілку на GitHub (швидко)

1. На GitHub натиснути на dropdown з "main" (вгорі ліворуч)
2. Вибрати гілку `2025-12-23-zsq5`
3. Тепер побачите папку `server/`

## Рішення 2: Змержити в main (рекомендовано)

Щоб `server/` з'явився в `main`:

### Варіант А: Через GitHub (найпростіше)
1. На GitHub перейти в гілку `2025-12-23-zsq5`
2. Натиснути **"Contribute"** → **"Open pull request"**
3. Base: `main` ← Compare: `2025-12-23-zsq5`
4. Натиснути **"Create pull request"**
5. Натиснути **"Merge pull request"**
6. Після merge - `server/` з'явиться в `main`

### Варіант Б: Через командний рядок
```bash
# Переключитися на main
git checkout main

# Змержити гілку
git merge 2025-12-23-zsq5

# Запушити в main
git push origin main
```

<hr style="border: none; border-top: 2px dotted #C9B36B; margin: 20px 0;">

## Після merge:

1. На GitHub гілка `main` матиме папку `server/`
2. Railway зможе побачити `server/` якщо підключений до `main`
3. Або налаштувати Railway на гілку `2025-12-23-zsq5`
