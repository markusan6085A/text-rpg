# 🔧 Виправлення реєстрації (Failed to fetch)

## Проблема
Реєстрація не працює з помилкою "Failed to fetch". Це означає, що фронтенд не може з'єднатися з бекендом.

## Причина
Відсутня або неправильно налаштована змінна `VITE_API_URL` на Vercel.

## Рішення

### 1. Отримати URL бекенду на Railway

1. Відкрийте Railway Dashboard: https://railway.app/dashboard
2. Знайдіть ваш проект (backend)
3. Відкрийте сервіс
4. Перейдіть на вкладку **"Settings"**
5. У розділі **"Networking"** знайдіть **"Public Domain"** або **"Custom Domain"**
6. Скопіюйте URL (наприклад: `https://text-rpg-production.up.railway.app`)

### 2. Додати змінну на Vercel

1. Відкрийте Vercel Dashboard: https://vercel.com/dashboard
2. Знайдіть проект `text-rpg` (або як він називається)
3. Перейдіть на **"Settings"** → **"Environment Variables"**
4. Натисніть **"Add New"**
5. Додайте змінну:
   - **Key:** `VITE_API_URL`
   - **Value:** URL з Railway **З ОБОВ'ЯЗКОВИМ `https://` на початку!** (наприклад: `https://text-rpg-production.up.railway.app`)
   - ⚠️ **ВАЖЛИВО:** Має бути `https://text-rpg-production.up.railway.app`, а НЕ просто `text-rpg-production.up.railway.app`
   - **Environment:** Виберіть всі (Production, Preview, Development)
6. Натисніть **"Save"**

### 3. Перезапустити deployment

1. У Vercel Dashboard перейдіть на **"Deployments"**
2. Знайдіть останній deployment
3. Натисніть на три крапки (⋯) → **"Redeploy"**
4. Або просто зробіть новий commit і push на GitHub

### 4. Перевірити бекенд

Переконайтеся, що бекенд працює на Railway:
- Відкрийте URL бекенду в браузері
- Має з'явитися JSON з повідомленням (наприклад, `{"message": "API is running"}`)
- Або перевірте endpoint: `https://ваш-railway-url.com/health` (якщо є)

### 5. Перевірити CORS

Якщо бекенд працює, але все ще є помилки:
- Переконайтеся, що CORS налаштовано правильно на бекенді
- Бекенд має дозволяти запити з домену Vercel (наприклад: `https://text-rpg.vercel.app`)

## Важливо

⚠️ **Змінна має називатися `VITE_API_URL`** (з префіксом `VITE_`), бо Vite додає тільки змінні з цим префіксом до білду!

## Перевірка після налаштування

1. Відкрийте сайт на Vercel
2. Спробуйте зареєструватися
3. Відкрийте DevTools (F12) → Console
4. Помилка "Failed to fetch" має зникнути
5. Реєстрація має працювати!
