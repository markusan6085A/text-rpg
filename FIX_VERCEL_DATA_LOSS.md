# 🔧 Виправлення: Дані пропадають при оновленні сторінки на Vercel

## ❌ Проблема:
При оновленні сторінки на Vercel (`textrpg-delta.vercel.app`) всі дані пропадають:
- Інвентар
- Скіли
- Професія
- Адена
- Все куплене

**Помилки в консолі:**
- `Failed to load resource: the server responded with a status of 500`
- `Failed to save hero via API: Error: Internal Server Error`

## 🔍 Причина:
1. **VITE_API_URL не налаштований на Vercel** - гра намагається підключитися до `localhost:3000`, який недоступний
2. **Backend недоступний** - API повертає 500 помилки
3. **Дані не зберігаються** - при збереженні також 500 помилка

## ✅ Рішення:

### Варіант 1: Налаштувати VITE_API_URL на Vercel (РЕКОМЕНДОВАНО)

1. **Визначити URL backend:**
   - Backend працює на VPS: `https://l2dop.com` (або ваш домен)

2. **Додати змінну на Vercel:**
   - Відкрийте Vercel Dashboard: https://vercel.com/dashboard
   - Знайдіть проект `textrpg-delta` (або як він називається)
   - Settings → Environment Variables
   - Натисніть **"+ Add"**
   - **Key:** `VITE_API_URL`
   - **Value:** `https://l2dop.com` (З ОБОВ'ЯЗКОВИМ `https://`!)
   - **Environment:** Виберіть всі (Production, Preview, Development)
   - Натисніть **"Save"**

3. **Redeploy на Vercel:**
   - Vercel Dashboard → Deployments
   - Знайдіть останній deployment
   - Натисніть на три крапки (⋯) → **"Redeploy"**
   - Або зробіть новий commit і push на GitHub

4. **Перевірити:**
   - Відкрийте гра на Vercel
   - Відкрийте консоль (F12)
   - Перевірте, чи немає помилок 500
   - Спробуйте оновити сторінку - дані мають зберігатися

### Варіант 2: Використати localStorage (Тимчасово)

Якщо backend недоступний, гра автоматично використовує localStorage як fallback. Але дані будуть зберігатися тільки локально в браузері.

**Обмеження:**
- Дані зберігаються тільки в цьому браузері
- При очищенні кешу дані втрачаються
- Немає синхронізації між пристроями

## 🔍 Перевірка:

1. **Перевірити backend:**
   - Відкрийте `https://ваш-railway-url.railway.app/health`
   - Має показати `{"status":"ok"}`

2. **Перевірити VITE_API_URL:**
   - Відкрийте гра на Vercel
   - F12 → Console
   - Введіть: `import.meta.env.VITE_API_URL`
   - Має показати URL backend (не `http://localhost:3000`)

3. **Перевірити збереження:**
   - Пограйте в гру
   - Додайте предмети в інвентар
   - Оновіть сторінку (F5)
   - Предмети мають залишитися

## ⚠️ Важливо:

- **VITE_API_URL має починатися з `https://`** (не `http://`)
- **Backend має бути доступний** (перевірте VPS / l2dop.com)
- **Після зміни VITE_API_URL потрібен redeploy** на Vercel

## 🎯 Після виправлення:

- ✅ Дані зберігаються в базу даних
- ✅ Дані синхронізуються між пристроями
- ✅ Дані не пропадають при оновленні сторінки
- ✅ Дані зберігаються навіть при очищенні кешу браузера
