# 🔧 Виправлення: Сервер падає після запуску

## ❌ Проблема

Сервер запускається (`Server started on http://0.0.0.0:3000`), але потім падає через 6 секунд з помилкою `SIGTERM`.

## 🔍 Можливі причини

1. **Prisma Client не згенерований** - потрібно запустити `prisma generate`
2. **Таблиці не створені** - потрібно запустити міграції
3. **DATABASE_URL неправильний** - перевірити connection string
4. **Помилка підключення до бази** - Prisma не може підключитися

## ✅ Рішення

### Перевірка 1: Чи правильно DATABASE_URL

1. Railway → **Settings** → **Variables**
2. Натисни на `DATABASE_URL` (три крапки → Edit)
3. Перевір формат:
   ```
   postgresql://postgres:ПАРОЛЬ@db.hstwsloooubalvpwasst.supabase.co:5432/postgres
   ```
4. Переконайся, що:
   - ✅ Є `postgresql://` на початку
   - ✅ Є пароль після `postgres:`
   - ✅ Немає `https://` (це не URL API!)
   - ✅ Правильний host (db.xxxxx.supabase.co)

### Перевірка 2: Prisma Client

1. Railway → **Settings** → **Deploy**
2. Перевір **Build Command:**
   ```
   npm install && npm run prisma:generate && npm run build
   ```
3. Має бути `prisma:generate` в build команді!

### Перевірка 3: Міграції (якщо потрібно)

Можливо, таблиці не створені. Потрібно запустити міграції:

**Варіант А: Через Railway CLI (якщо встановлено)**
```bash
railway run npm run prisma:migrate:deploy
```

**Варіант Б: Додати в Build Command**
Оновити Build Command на Railway:
```
npm install && npm run prisma:generate && npx prisma migrate deploy && npm run build
```

### Перевірка 4: Логи детальніші

1. Railway → **Logs**
2. Прокрути вниз - можливо, є детальніша помилка перед SIGTERM
3. Шукай:
   - `PrismaClientInitializationError`
   - `Can't reach database server`
   - `Connection refused`
   - `relation "Account" does not exist`

## 🎯 Швидке рішення

1. **Перевір Build Command на Railway:**
   - Railway → Settings → Deploy
   - Build Command має бути: `npm install && npm run prisma:generate && npm run build`

2. **Перевір DATABASE_URL:**
   - Railway → Settings → Variables
   - Переконайся, що формат правильний (без https://)

3. **Якщо все ще падає:**
   - Надішли детальніші логи (прокрути вниз в Logs)
   - Шукай помилки перед "Stopping Container"
