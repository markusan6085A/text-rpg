# 🔧 Виправлення помилки: DATABASE_URL не знайдено

## ❌ Проблема

В логах Railway помилка:
```
Environment variable not found: DATABASE_URL
```

Це означає, що на Railway не встановлено змінну `DATABASE_URL`, тому Prisma не може підключитися до бази даних.

---

## ✅ Рішення

### Крок 1: Отримати DATABASE_URL з Supabase

1. Відкрийте **Supabase Dashboard**: https://supabase.com/dashboard
2. Виберіть ваш проект
3. Перейдіть: **Settings** → **Database**
4. У розділі **Connection String** знайдіть:
   - **URI** (з паролем) - використовуйте цей
   - Або **Connection pooling** (якщо є)
5. Скопіюйте повний connection string

**Приклад:**
```
postgresql://postgres:[YOUR-PASSWORD]@db.xxxxx.supabase.co:5432/postgres
```

⚠️ **ВАЖЛИВО:** Замініть `[YOUR-PASSWORD]` на реальний пароль з Supabase!

### Крок 2: Знайти пароль (якщо не пам'ятаєте)

Якщо не пам'ятаєте пароль:

1. Supabase Dashboard → **Settings** → **Database**
2. Знайдіть розділ **Database Password**
3. Якщо пароль не видно, натисніть **Reset Database Password**
4. Скопіюйте новий пароль
5. Оновіть connection string з новим паролем

### Крок 3: Додати DATABASE_URL на Railway

1. Відкрийте **Railway Dashboard**: https://railway.app/dashboard
2. Знайдіть ваш проект (backend)
3. Відкрийте сервіс
4. Перейдіть: **Settings** → **Variables**
5. Натисніть **Add Variable** (або **+ New Variable**)
6. Додайте змінну:
   - **Key:** `DATABASE_URL`
   - **Value:** Вставте connection string з Supabase (з паролем!)
   - **Environment:** Виберіть всі (Production, Preview, Development)
7. Натисніть **Save**

### Крок 4: Перезапустити deployment

Railway автоматично перезапустить сервіс після збереження змінної.

Якщо потрібно вручну:
1. Railway Dashboard → **Deployments**
2. Знайдіть останній deployment
3. Натисніть на три крапки (⋯) → **Redeploy**

### Крок 5: Перевірити

1. Зачекайте 1-2 хвилини (поки Railway перезапустить)
2. Перевірте логи Railway:
   - Railway Dashboard → **Logs**
   - Помилка `DATABASE_URL not found` має зникнути
3. Спробуйте реєстрацію знову на сайті

---

## ⚠️ Важливі примітки

1. **Пароль в URL:** Connection string має містити пароль:
   ```
   postgresql://postgres:ВАШ_ПАРОЛЬ@db.xxxxx.supabase.co:5432/postgres
   ```

2. **URL Encoding:** Якщо пароль містить спеціальні символи, вони можуть потребувати URL encoding:
   - `@` → `%40`
   - `#` → `%23`
   - `%` → `%25`
   - і т.д.

3. **Connection Pooling:** Якщо Supabase надає Connection pooling URL, можна використати його замість звичайного URI (якщо це окремий URL).

4. **Перевірка:** Після додавання змінної, перевірте логи Railway - помилка `DATABASE_URL not found` має зникнути.

---

## 🎯 Після виправлення

Після додавання `DATABASE_URL`:
- ✅ Prisma зможе підключитися до бази даних
- ✅ Реєстрація має працювати
- ✅ Логін має працювати
- ✅ Створення персонажа має працювати

---

## 📚 Додаткова інформація

- Supabase Database Settings: https://supabase.com/dashboard/project/_/settings/database
- Railway Environment Variables: https://railway.app/dashboard/[project]/variables
- Prisma DATABASE_URL: https://www.prisma.io/docs/concepts/database-connectors/postgresql
