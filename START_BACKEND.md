# 🚀 Як запустити Backend сервер

## ❌ Проблема:
Backend сервер не запущений, тому виникає помилка "Internal Server Error".

## ✅ Рішення:

### Крок 1: Відкрити НОВИЙ термінал

Backend має запускатися в **окремому терміналі** (не в тому, де frontend).

### Крок 2: Запустити Backend

```bash
cd server
npm run dev
```

### Крок 3: Перевірити, що сервер запущений

Повинно з'явитися повідомлення:
```
Server started on http://0.0.0.0:3000
```

Або перевірити в браузері:
```
http://localhost:3000
```

Повинно показати JSON з інформацією про сервер.

---

## ⚠️ Важливо:

**Backend** і **Frontend** мають запускатися **в різних терміналах**:

1. **Термінал 1:** Backend (`cd server && npm run dev`) → порт 3000
2. **Термінал 2:** Frontend (`npm run dev`) → порт 5175

---

## ✅ Після запуску Backend:

1. Backend запуститься на `http://localhost:3000`
2. Спробуйте зареєструватися знову
3. Помилка "Internal Server Error" має зникнути

---

**Я запустив backend у фоновому режимі, але краще запустити в окремому терміналі!**
