# План інтеграції Backend API

## ✅ Що вже зроблено:

1. ✅ API client (`src/utils/api.ts`) - готовий
2. ✅ Auth store (`src/state/authStore.ts`) - готовий
3. ✅ Character store (`src/state/characterStore.ts`) - готовий
4. ✅ `heroPersistence.ts` - оновлено для використання API
5. ✅ `heroLoadAPI.ts` - створено функцію для завантаження з API

## ⏳ Що потрібно зробити:

### 1. Оновити `heroStore.ts`
- Додати async `loadHeroFromAPI()` виклик
- Змінити `loadHero()` на async (або створити окрему функцію)

### 2. Оновити `Register.tsx`
- Використовувати `register()` API
- Використовувати `createCharacter()` API
- Зберегти token в authStore
- Зберегти characterId в characterStore

### 3. Оновити `Landing.tsx`
- Використовувати `login()` API
- Зберегти token в authStore
- Завантажити список персонажів
- Завантажити вибраного персонажа

### 4. Оновити `App.tsx`
- Ініціалізувати authStore
- Ініціалізувати characterStore
- Викликати async `loadHeroFromAPI()` при ініціалізації

## 📝 Крок за кроком:

1. **Оновити `Register.tsx`** - реєстрація через API
2. **Оновити `Landing.tsx`** - логін через API
3. **Оновити `App.tsx`** - ініціалізація stores та завантаження героя
4. **Тестування** - перевірити, що все працює
