# 📁 Налаштування Root Directory в Railway

## Що потрібно зробити:

### Крок 1: Додати Root Directory
1. На сторінці Settings → Source
2. Знайти посилання **"Add Root Directory"** (під "Source Repo")
3. Натиснути на посилання
4. Ввести: `server`
5. Зберегти

### Крок 2: Перевірити Build & Deploy
1. Перейти в **Settings** → **Build & Deploy** (або через quick navigation справа)
2. Перевірити:
   - **Build Command:** `npm install && npm run prisma:generate && npm run build`
   - **Start Command:** `npm start`

### Крок 3: Додати Environment Variables
1. Перейти в **Settings** → **Variables**
2. Додати:
   - `DATABASE_URL` = (з `server/.env`)
   - `JWT_SECRET` = (згенерувати довгий ключ)

### Крок 4: Зберегти
1. Railway автоматично перезапустить деплой
2. Перевірити логи - тепер має компілювати з `server/src/`, а не з кореня

---

## Після налаштування:

✅ Railway буде деплоїти з папки `server/`
✅ Build Command буде запускатися в папці `server/`
✅ TypeScript компіляція з `server/src/` → `server/dist/`
✅ Start Command запустить `node dist/index.js`
