# Копіюй як .env і підстав свої значення. Не коміть .env з секретами.

# URL бекенду. Для локальної розробки зазвичай http://localhost:3000
# У production на Vercel можна не ставити — клієнт використовує /api (rewrite на api.l2dop.com)
VITE_API_URL=http://localhost:3000
