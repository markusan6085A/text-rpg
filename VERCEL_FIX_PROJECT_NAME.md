# 🔧 Виправлення назви проекту в Vercel

## Проблема:
Червона помилка: "The name contains invalid characters..."

## Рішення:

1. Знайти поле **"Project Name"** (вище Environment Variables)
2. Замінити назву на:
   - `text-rpg` (або)
   - `text_rpg` (або)
   - `textrpg`

**ВАЖЛИВО:**
- Тільки букви, цифри та підкреслення
- Дефіси також дозволені (як `text-rpg`)
- НЕ починається з цифри

3. Після зміни назви помилка має зникнути
4. Натиснути **"Deploy"**

---

## Якщо помилка залишається:

1. Перевірити, що в назві немає:
   - Спеціальних символів (крім `-` та `_`)
   - Пробілів
   - Не починається з цифри

2. Спробувати просту назву: `textrpg`
