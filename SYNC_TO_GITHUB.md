# 🔄 Синхронізація локального коду з GitHub

Якщо локальна папка відрізняється від GitHub репозиторію, потрібно синхронізувати.

## Варіант 1: Push всіх змін на GitHub (рекомендовано)

Якщо локально є всі файли (включаючи `server/`), а на GitHub їх немає:

### Крок 1: Перевірити поточний стан
```bash
git status
```

### Крок 2: Додати всі файли
```bash
git add .
```

### Крок 3: Закомітити
```bash
git commit -m "Add backend server and update project"
```

### Крок 4: Запушити на GitHub
```bash
git push origin main
```

Якщо ви на іншій гілці:
```bash
git push origin 2025-12-23-zsq5
```

---

## Варіант 2: Якщо є конфлікти

Якщо GitHub має зміни, яких немає локально:

### Крок 1: Отримати зміни з GitHub
```bash
git fetch origin
```

### Крок 2: Подивитися різницю
```bash
git log HEAD..origin/main
```

### Крок 3: Змержити зміни
```bash
git merge origin/main
```

Або якщо хочете перезаписати GitHub версією:
```bash
git pull origin main
```

---

## Варіант 3: Створити новий коміт з усіма змінами

### Крок 1: Перевірити що потрібно додати
```bash
git status
```

### Крок 2: Додати всі зміни
```bash
git add .
```

### Крок 3: Перевірити що буде закомічено
```bash
git status
```

### Крок 4: Закомітити
```bash
git commit -m "Sync local changes with GitHub - add server and all updates"
```

### Крок 5: Запушити
```bash
git push origin main
```

---

## Якщо потрібно перезаписати GitHub (увага - це видалить зміни на GitHub!)

**⚠️ УВАГА:** Це видалить всі зміни на GitHub, яких немає локально!

```bash
git push origin main --force
```

**НЕ використовуйте --force, якщо не впевнені!**

---

## Перевірка після push

1. Відкрити GitHub репозиторій в браузері
2. Перевірити, що з'явилася папка `server/`
3. Перевірити, що всі файли на місці
