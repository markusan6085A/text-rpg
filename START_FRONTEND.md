# 🎮 Запуск Frontend (Гри)

## 📍 Важливо:

**Backend** працює на: `http://localhost:3000`  
**Frontend** (гра) працює на: `http://localhost:5173` (або інший порт)

<hr style="border: none; border-top: 2px dotted #C9B36B; margin: 20px 0;">

## 🚀 Як запустити:

1. **Відкрити новий термінал** (не той, де працює backend)

2. **Перейти в кореневу папку проєкту:**
   ```bash
   cd "C:\Users\KDFX Modes\Desktop\text-rpg"
   ```

3. **Запустити frontend:**
   ```bash
   npm run dev
   ```

4. **Чекати на повідомлення в терміналі:**
   ```
   ➜  Local:   http://localhost:5173/
   ➜  Network: use --host to expose
   ```

5. **Відкрити в браузері посилання з терміналу** (зазвичай `http://localhost:5173`)

<hr style="border: none; border-top: 2px dotted #C9B36B; margin: 20px 0;">

## ⚠️ Якщо не працює:

- Перевірте, що ви в правильній папці (корінь проєкту, не `server/`)
- Перевірте, що порт 5173 не зайнятий
- Якщо порт зайнятий, Vite автоматично використає інший (перевірте в терміналі)
